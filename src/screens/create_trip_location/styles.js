import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    safeAreaView: { width: '100%', height: '100%', backgroundColor: 'rgb(245,245,248)', elevation: 5, borderRadius: 10, padding: 10 },
    keyBoardAvoidView: { flex: 1, backgroundColor: 'rgb(0,53,108)' },

});
